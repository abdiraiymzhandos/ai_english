# Fixture/content workflow — deprecated pointer

The maintained content model, safe aggregate snapshot, fixture hazards, validation rules, and proposed import/export workflow are in [docs/CONTENT_OPERATIONS.md](docs/CONTENT_OPERATIONS.md).

Current tracked fixtures are invalid JSON and several contain private runtime data. Do not load, refresh, or publish them before task `CONTENT-001`.
