# Voice lesson README — deprecated pointer

The maintained AI/model/prompt/privacy/cost map is [docs/AI_INTEGRATIONS.md](docs/AI_INTEGRATIONS.md). Transport and event invariants are in [docs/REALTIME_AND_QUIZ_CONTRACTS.md](docs/REALTIME_AND_QUIZ_CONTRACTS.md).

Important current-tree warning: a pre-existing user modification routes the learner voice endpoint to a social-content prompt that ignores the selected lesson. Intent is `Needs product-owner confirmation`; do not silently overwrite it. Use task `BUG-002` for a scoped resolution.
